version: "3.9"
services:
  C:
    image: "alepart/ipfs_cluster"
    network_mode: "net1"
    volumes:
      - node_1:/root:rw
      - node_1_data:/root/.ipfs:rw
    command: "/root/start.sh"
volumes:
  node_1:
    external: true
  node_1_data:

    
